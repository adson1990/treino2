<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="projeto.treino.app.com.br.treino2">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:supportsRtl="true"
        android:theme="@style/AppTheme">
        //activity login (1 tela)
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        //activity boas vindas (2 tela)
        <activity android:name=".TelaBoasVindas">
        </activity>

        //grid view (tela 2)
        <activity android:name=".ExGridView">
        </activity>

        //gallery (tela 3)
        <activity android:name=".ExGallery">
        </activity>

        //mostrar host activity (tela propria)
        <activity android:name=".MostrarHostActivity"/>

    </application>

</manifest>
